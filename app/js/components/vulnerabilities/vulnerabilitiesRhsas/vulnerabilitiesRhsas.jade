.page-vulnerabilities-rhsa(ng-if='!loading')
  section.header
    page-header(title="{{:: package_name}}")
    .description
      p TODO: Insert Package description in vmaas.js mock data

  table-filters
    search-box(placeholder="{{'Search RHSAs' | translate}}", on-search='search(model)')
    rhsa-severity-select

  section.content
    table
      thead
        tr
          th.min.fit-content.nowrap.sortable(translate) RHSA ID
          th.min.fit-content.nowrap.sortable(translate) Severity
          th.text-center.min.fit-content.nowrap.sortable(translate) CVEs
          th.text-center.min.fit-content.nowrap.sortable(translate) Systems Affected
          th.min.fit-content.nowrap.sortable(translate) Issued
          th.text-center.min.fit-content.nowrap.sortable(translate) Rule Hit
      tbody
        tr(ng-repeat="rhsa in rhsas")
          td.min.fit-content.nowrap(translate) {{:: rhsa.id}}
          td.fit-content.nowrap(translate)
            .icon-inline
              i.fa.fa-shield(ng-class="{{'rhsa.severity'}}")
              span {{:: rhsa.severity}}
          td.text-center.fit-content.nowrap(translate) {{rhsa.cves.length}}
          td.text-center.fit-content.nowrap(translate) {{rhsa.systems_affected}}
          td.fit-content.nowrap(translate) {{:: rhsa.issued}}
          td.text-center.fit-content.nowrap
            i.fa.fa-stethoscope(ng-class="{hasRuleHit: rhsa.rule_hits > 0}")

